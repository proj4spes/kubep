[defaults]
host_key_checking = no
record_host_keys = no
jinja2_extensions = jinja2.ext.do
timeout = 15
gathering = smart
roles_path = roles
forks = 100
library = ./library

[ssh_connection]
ssh_args = -o ControlMaster=no -o ControlPersist=30m
scp_if_ssh = True
pipelining = True
